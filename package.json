{
  "name": "template-demo",
  "version": "1.0.0",
  "description": "A guided tour of Kirigami — built with Kirigami.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.2.3"
  },
  "scripts": {
    "build": "kiri build",
    "watch": "kiri watch",
    "export": "kiri export"
  },
  "devDependencies": {
    "@kirigami/canva": "^2.5.1",
    "@kirigami/kirigami": "^1.5.5",
    "@kirigami/plugin-embed": "^0.1.3",
    "@kirigami/plugin-extlink": "^0.1.2",
    "@kirigami/plugin-highlight": "^0.1.5"
  }
}
